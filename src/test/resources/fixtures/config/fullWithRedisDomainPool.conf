penfold {

  publicUrl = "http://localhost:9762"

  httpPort = 9762

  authentication {
    username = user
    password = secret
  }

  domainRedisConnectionPool {
    host = localhost
    port = 6380
    database = 0
    poolSize = 10
    password = secret
  }

  readStoreRedisConnectionPool {
    host = localhost
    port = 6380
    database = 1
    poolSize = 100
    password = secret
  }

  readStoreIndexes = [
    {
      name = index1
      fields = [{key = field1, path = payload / inner / field1}]
    },
    {
      name = index2
      fields = [{key = field1, path = payload / inner / field1}, {key = field2, path = payload / field2}]
    }
  ]

  triggeredCheckFrequency = 1 minute

}