penfold {

  publicUrl = "http://localhost:9762"

  httpPort = 9762

  authentication {
    username = user
    password = secret
  }

  domainJdbcConnectionPool {
    driver = org.hsqldb.jdbcDriver
    url = "jdbc:hsqldb:mem:penfold;sql.syntax_mys=true"
    username = user
    password = secret
    poolSize = 10
  }

  readStoreJdbcConnectionPool {
    driver = org.hsqldb.jdbcDriver
    url = "jdbc:hsqldb:mem:penfold;sql.syntax_mys=true"
    username = user
    password = secret
    poolSize = 10
  }

  readStoreAliases = {
    alias1 = path1
    alias2 = path2
  }

  sortOrdering = {
    waiting = Desc
    ready = Desc
    started = Asc
    closed = Asc
  }

  pageSize = 25

  eventSync = 2 minutes

  triggeredCheckFrequency = 1 minute

  taskArchiver = {
    timeoutPayloadPath = "archiveTimeout"
    checkFrequency = 1 minute
  }

  readyTaskAssignmentTimeout = {
    timeoutPayloadPath = "assignmentTimeout"
    checkFrequency = 2 minute
  }

}